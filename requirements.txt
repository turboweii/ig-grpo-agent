# ========================================================================
# IG-GRPO Agent 依赖清单
# ========================================================================
#
# 安装顺序建议:
#   1. pip install torch==2.7.0 torchvision==0.22.0 --index-url https://download.pytorch.org/whl/cu126
#   2. pip install vllm==0.9.2
#   3. pip install flash-attn --no-build-isolation
#   4. pip install -r requirements.txt
#   5. pip install -e /path/to/tau-bench
#
# 环境变量:
#   export VLLM_USE_V1=1
#   export CUDA_VISIBLE_DEVICES=0,1,2,3

# ---- PyTorch (CUDA 12.6) ----
torch==2.7.0
torchvision==0.22.0
torchaudio==2.7.0

# ---- vLLM (V1 Engine) ----
# 必须设置 VLLM_USE_V1=1
vllm==0.9.2

# ---- Transformers / PEFT ----
transformers>=4.51.0,<4.52.0
peft==0.18.1
trl>=0.11.0

# ---- 分布式 / 并行 ----
ray==2.55.1
accelerate>=0.34.0
deepspeed>=0.15.0

# ---- 注意力优化 ----
# flash-attn 需从源码编译安装:
#   pip install flash-attn --no-build-isolation
flash-attn>=2.4.0

# ---- 数据 ----
datasets>=3.0.0
pandas>=2.2.0
pyarrow>=17.0.0

# ---- 配置 ----
hydra-core>=1.3.2
omegaconf>=2.3.0

# ---- API 客户端 ----
openai>=1.40.0

# ---- 工具 ----
tqdm
pyyaml>=6.0
sentencepiece>=0.2.0
protobuf>=5.0.0

# ---- 量化 ----
bitsandbytes>=0.45.0

# ---- 日志 / 实验管理 ----
swanlab>=0.7.0
tensorboard>=2.20.0

# ---- Bloom Filter ----
pybloom-live-mirror>=0.0.1

# ---- 评测 ----
scikit-learn>=1.3.0
sentence-transformers>=2.2.0

# ---- 可视化 ----
matplotlib>=3.7.0
seaborn>=0.12.0

# ---- 测试 ----
pytest>=8.0
pytest-asyncio>=0.24

# ---- 其他工具 ----
rich>=13.0.0
typer>=0.9.0
